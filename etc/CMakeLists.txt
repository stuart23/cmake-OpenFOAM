set( CONFIG_FILES
	controlDict
	cellModels
)

set( CONFIG_DIRECTORIES
	codeTemplates
)

install( FILES ${CONFIG_FILES}
	DESTINATION ${CMAKE_INSTALL_PREFIX}/etc )

install( DIRECTORY ${CONFIG_DIRECTORIES} 
	DESTINATION ${CMAKE_INSTALL_PREFIX}/etc )
